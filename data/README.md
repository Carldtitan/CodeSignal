# Local state

The backend creates `codelab-state.json` in this directory on first launch. It stores editor code, testcases, notes, progress, submissions, preferences, and the active session. The state file is intentionally ignored by Git so each clone keeps its own private practice history.
